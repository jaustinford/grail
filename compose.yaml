---
services:

  ############################################################
  # backup_raidvol
  ############################################################

  backup_raidvol:
    image: docker-hosted.proxy-int.pendragonlab.com/elise/grail:main
    entrypoint: python src
    profiles:
      - backup-raidvol

    environment:
      - TZ=America/Denver
      - BACKUP_OBJECT=${BACKUP_OBJECT}

    volumes:
      - type: volume
        source: raid_vol
        target: /src-backup-path
        read_only: false

      - type: bind
        source: /mnt/usb-backup/raidvol
        target: /dst-backup-path
        read_only: false

    networks:
      - grail

    logging:
      options:
        max-size: 10m
        max-file: '10'

  ############################################################
  # restore_raidvol
  ############################################################

  restore_raidvol:
    image: docker-hosted.proxy-int.pendragonlab.com/elise/grail:main
    entrypoint: python src
    profiles:
      - restore-raidvol

    environment:
      - TZ=America/Denver
      - BACKUP_OBJECT=${BACKUP_OBJECT}

    volumes:
      - type: bind
        source: /mnt/usb-backup/raidvol
        target: /src-backup-path
        read_only: false

      - type: volume
        source: raid_vol
        target: /dst-backup-path
        read_only: false

    networks:
      - grail

    logging:
      options:
        max-size: 10m
        max-file: '10'

  ############################################################
  # backup_appvol
  ############################################################

  backup_appvol:
    image: docker-hosted.proxy-int.pendragonlab.com/elise/grail:main
    entrypoint: python src
    profiles:
      - backup-appvol

    environment:
      - TZ=America/Denver
      - BACKUP_OBJECT=appvol

    volumes:
      - type: bind
        source: /mnt/dfs_lvm/lab_dfs
        target: /src-backup-path
        read_only: false

      - type: bind
        source: /mnt/usb-backup/appvol
        target: /dst-backup-path
        read_only: false

    networks:
      - grail

    logging:
      options:
        max-size: 10m
        max-file: '10'

  ############################################################
  # restore_appvol
  ############################################################

  restore_appvol:
    image: docker-hosted.proxy-int.pendragonlab.com/elise/grail:main
    entrypoint: python src
    profiles:
      - restore-appvol

    environment:
      - TZ=America/Denver
      - BACKUP_OBJECT=appvol

    volumes:
      - type: bind
        source: /mnt/usb-backup/appvol
        target: /src-backup-path
        read_only: false

      - type: bind
        source: /mnt/dfs_lvm/lab_dfs
        target: /dst-backup-path
        read_only: false

    networks:
      - grail

    logging:
      options:
        max-size: 10m
        max-file: '10'

###########################################################
# resources
###########################################################

volumes:
  raid_vol:
    driver_opts:
      type: cifs
      o: "\
        username=${RAID_VOL_USERNAME},\
        password=${RAID_VOL_PASSWORD},\
        uid=${LOCAL_USER_UID},\
        gid=${LOCAL_USER_GID},\
        iocharset=utf8"
      device: //${RAID_VOL_HOST}/${RAID_VOL_SHARE}

networks:
  grail:
    name: grail
